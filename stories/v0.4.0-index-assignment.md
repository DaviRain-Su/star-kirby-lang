# Story: v0.4.0 索引赋值和性能优化

> 实现索引赋值操作和性能基准测试框架

## 目标

1. 实现数组和哈希的索引赋值操作 (`arr[0] = value`, `hash["key"] = value`)
2. 添加性能基准测试框架
3. 确保所有功能有完整的测试覆盖

## 验收标准

### 索引赋值 - Lexer (lexer.zig)

- [ ] 现有 token 支持已足够（无需修改）

### 索引赋值 - AST (ast.zig)

- [ ] 添加 IndexAssignment 结构体
- [ ] 在 Statement 联合体中添加 index_assignment 变体
- [ ] 单元测试

### 索引赋值 - Parser (parser.zig)

- [ ] 实现 parseIndexAssignment 函数
- [ ] 在 parseStatement 中处理索引赋值
- [ ] 支持 `arr[index] = value` 语法
- [ ] 支持 `hash[key] = value` 语法
- [ ] 支持嵌套索引赋值 `arr[0][1] = value`
- [ ] 单元测试

### 索引赋值 - Evaluator (evaluator.zig)

- [ ] 实现 evalIndexAssignment 函数
- [ ] 支持数组元素赋值
- [ ] 支持哈希键值对赋值
- [ ] 边界检查和错误处理
- [ ] 单元测试

### 索引赋值 - Object (object.zig)

- [ ] 确保 Array 和 Hash 对象支持修改
- [ ] 验证内存管理正确

### 索引赋值 - REPL 集成

- [ ] REPL 支持索引赋值语法
- [ ] 集成测试

### 性能基准测试 (可选)

- [ ] 创建 benchmarks/ 目录
- [ ] 实现基本性能测试框架
- [ ] 添加算术运算基准
- [ ] 添加函数调用基准
- [ ] 添加递归函数基准

### 集成

- [ ] root.zig 导出更新（如需要）
- [ ] 文档更新
- [ ] 所有测试通过 (zig build test)
- [ ] 无内存泄漏

## 技术设计

### 索引赋值语法

```monkey
// 数组索引赋值
let arr = [1, 2, 3];
arr[0] = 10;        // arr 变为 [10, 2, 3]

// 哈希键赋值
let h = {"a": 1};
h["b"] = 2;         // h 变为 {"a": 1, "b": 2}

// 嵌套索引赋值
let matrix = [[1, 2], [3, 4]];
matrix[0][1] = 10;  // matrix 变为 [[1, 10], [3, 4]]
```

### AST 结构设计

```zig
pub const IndexAssignment = struct {
    token: Token,           // '[' token
    left: *Expression,      // 被索引的表达式 (arr, hash, etc.)
    index: *Expression,     // 索引表达式
    value: *Expression,     // 赋值的值
};
```

### 解析策略

索引赋值需要在解析器中特殊处理：
1. 当遇到 `identifier[index]` 后跟 `=` 时，解析为索引赋值
2. 否则解析为普通索引表达式

## 完成状态

- 开始日期: 2026-01-03
- 完成日期: 未完成
- 状态: 🔨 进行中

## 进度记录

### 2026-01-03 Session
- 创建 v0.4.0 Story 文件
- 开始实现索引赋值功能
