# Story: v0.7.0 工具增强和 I/O 支持

> 添加文件 I/O、脚本执行、输出优化和更多字符串操作

## 目标

1. 实现文件读写内置函数
2. 支持从文件执行 Monkey 脚本
3. 优化 Array 和 Hash 的字符串表示
4. 添加更多字符串操作函数
5. 添加注释支持
6. 改进 REPL 体验

## 验收标准

### 1. 文件 I/O (builtins.zig)

- [x] `readFile(path)` - 读取文件内容为字符串
- [x] `writeFile(path, content)` - 写入字符串到文件
- [x] `appendFile(path, content)` - 追加内容到文件
- [x] `fileExists(path)` - 检查文件是否存在

#### 测试
- [x] 读取存在的文件
- [x] 读取不存在的文件返回错误
- [x] 写入新文件
- [x] 追加到现有文件

### 2. 脚本执行 (main.zig)

- [x] `zig build run -- <file.monkey>` 执行脚本文件
- [x] 支持 `.monkey` 和 `.mk` 扩展名
- [x] 脚本执行错误报告

#### 测试
- [x] 执行简单脚本
- [x] 执行包含函数的脚本
- [x] 脚本语法错误报告

### 3. 输出优化 (object.zig)

- [x] Array inspect 显示 `[1, 2, 3]` 而非 `[array]`
- [x] Hash inspect 显示 `{"a": 1, "b": 2}` 而非 `{hash}`
- [x] 嵌套结构正确显示

#### 测试
- [x] 空数组 `[]`
- [x] 嵌套数组 `[[1, 2], [3, 4]]`
- [x] 空哈希 `{}`
- [x] 嵌套哈希

### 4. 字符串操作 (builtins.zig)

- [x] `split(str, delimiter)` - 分割字符串
- [x] `join(array, delimiter)` - 连接数组元素
- [x] `trim(str)` - 去除首尾空白
- [x] `upper(str)` - 转大写
- [x] `lower(str)` - 转小写
- [x] `contains(str, substr)` - 检查包含
- [x] `replace(str, old, new)` - 替换字符串
- [x] `charAt(str, index)` - 获取字符
- [x] `substring(str, start, end)` - 获取子串
- [x] `indexOf(str, substr)` - 查找子串索引

#### 测试
- [x] 每个函数有测试
- [x] 边界情况测试

### 5. 注释支持 (lexer.zig)

- [x] 单行注释 `// comment`
- [x] 多行注释 `/* comment */`

### 6. 集成测试

- [x] `zig build test` 通过
- [x] `zig build bench` 通过
- [x] 无内存泄漏

### 7. 文档

- [x] CHANGELOG.md 更新
- [x] ROADMAP.md 更新
- [x] Story 文件更新
- [x] main.zig --help 更新

## 技术设计

### 文件 I/O 实现

```zig
/// 读取文件
fn builtinReadFile(allocator: std.mem.Allocator, args: []Object) anyerror!Object {
    if (args.len != 1) {
        return try object_mod.makeError(allocator, "wrong number of arguments to `readFile`. want=1");
    }
    if (args[0].objectType() != .string) {
        return try object_mod.makeError(allocator, "argument to `readFile` must be STRING");
    }
    
    const path = args[0].string.value;
    const file = std.fs.cwd().openFile(path, .{}) catch {
        return try object_mod.makeError(allocator, "could not open file");
    };
    defer file.close();
    
    const content = file.readToEndAlloc(allocator, 10 * 1024 * 1024) catch {
        return try object_mod.makeError(allocator, "could not read file");
    };
    
    return object_mod.makeStringOwned(allocator, content);
}
```

### 脚本执行

```zig
// main.zig
if (std.mem.endsWith(u8, input, ".monkey") or std.mem.endsWith(u8, input, ".mk")) {
    // Read and execute file
    const content = try std.fs.cwd().readFileAlloc(allocator, input, 10 * 1024 * 1024);
    defer allocator.free(content);
    const result = try repl.eval(content);
    // ...
}
```

### Array/Hash Inspect

```zig
// object.zig
.array => |arr| blk: {
    var buffer = std.ArrayList(u8).init(allocator);
    try buffer.append(allocator, '[');
    for (arr.elements, 0..) |elem, i| {
        if (i > 0) try buffer.appendSlice(allocator, ", ");
        const elem_str = try elem.inspect(allocator);
        defer allocator.free(elem_str);
        try buffer.appendSlice(allocator, elem_str);
    }
    try buffer.append(allocator, ']');
    break :blk try buffer.toOwnedSlice(allocator);
}
```

### 注释处理

```zig
// lexer.zig
fn skipWhitespaceAndComments(self: *Self) void {
    while (true) {
        self.skipWhitespace();
        
        // Single-line comment: // ...
        if (self.ch == '/' and self.peekChar() == '/') {
            while (self.ch != '\n' and self.ch != 0) {
                self.readChar();
            }
            continue;
        }
        
        // Multi-line comment: /* ... */
        if (self.ch == '/' and self.peekChar() == '*') {
            self.readChar(); // consume /
            self.readChar(); // consume *
            while (!(self.ch == '*' and self.peekChar() == '/') and self.ch != 0) {
                self.readChar();
            }
            if (self.ch != 0) {
                self.readChar(); // consume *
                self.readChar(); // consume /
            }
            continue;
        }
        
        break;
    }
}
```

## 测试用例

```monkey
// 文件 I/O
writeFile("test.txt", "Hello, World!");
let content = readFile("test.txt");
puts(content);  // Hello, World!

// 字符串操作
split("a,b,c", ",");           // ["a", "b", "c"]
join(["a", "b", "c"], "-");    // "a-b-c"
trim("  hello  ");             // "hello"
upper("hello");                // "HELLO"
lower("HELLO");                // "hello"
contains("hello", "ell");      // true
replace("hello", "l", "L");    // "heLLo"
charAt("hello", 0);            // "h"
substring("hello", 1, 4);      // "ell"
indexOf("hello", "ll");        // 2

// 改进的输出
puts([1, 2, 3]);               // [1, 2, 3]
puts({"a": 1});                // {"a": 1}

// 注释支持
// 这是单行注释
/* 这是
   多行注释 */
```

## 完成状态

- 开始日期: 2026-01-03
- 完成日期: 2026-01-03
- 状态: ✅ 已完成

## 进度记录

### 2026-01-03 - 规划

- 创建 Story 文件
- 规划 v0.7.0 功能范围

### 2026-01-03 - 实现

- 实现文件 I/O 内置函数 (readFile, writeFile, appendFile, fileExists)
- 实现字符串操作函数 (split, join, trim, upper, lower, contains, replace, charAt, substring, indexOf)
- 优化 Array 和 Hash 的 inspect 输出
- 添加注释支持到 lexer
- 实现脚本文件执行功能
- 更新 main.zig 版本号和 help 信息

### 2026-01-03 - 验证

- 所有测试通过 (`zig build test`)
- 所有基准测试通过 (`zig build bench`)
- 所有新功能手动测试验证通过
- 文档更新完成
